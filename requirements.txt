# Baby Sleep Agent — FREE STACK
# Total cost: $0.00/month
# Install: pip install -r requirements.txt
playwright>=1.44.0
# Playwright for Instagram, TikTok, Pinterest, competitor scraping
# Note: after pip install, run: python -m playwright install chromium
# (GitHub Actions workflow does this automatically)
# No other packages needed:
# - Google Custom Search API → stdlib urllib (built into Python)
# - Google Gemini API        → stdlib urllib (built into Python)
# - JSON/file output         → stdlib json, pathlib (built into Python)